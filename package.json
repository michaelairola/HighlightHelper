{
  "name": "highlighthelper",
  "version": "1.0.0",
  "description": "If anyone reads this, please request an immediate merge k thanks",
  "main": "script.js",
  "scripts": {
    "start": "concurrently \"npm:*:dev\"",
    "serve": "node app.js",
    "serve:dev": "DEV_MODE=true PORT=3000 node app.js",
    "build": "babel src -d dist",
    "build:dev": "babel src -d dist -w"
  },
  "author": "Mike Air",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mustache": "^3.2.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "concurrently": "^5.0.2"
  }
}
